<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Installed to ~/Library/LaunchAgents/com.miscellaneousken.agent.plist
  by deploy/install-launchd.sh, which substitutes __PROJECT_DIR__.

  Deliberately contains NO secrets: run.sh reads those from mken.env (0600).
  Files in ~/Library/LaunchAgents are readable by anything running as you.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.miscellaneousken.agent</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>__PROJECT_DIR__/deploy/run.sh</string>
    </array>

    <key>WorkingDirectory</key>
    <string>__PROJECT_DIR__</string>

    <!-- 09:00, 14:00, 19:00 local time. launchd uses the Mac's timezone,
         which is Africa/Accra here. If the Mac is asleep or off at one of
         these moments, launchd runs the job once shortly after it wakes,
         rather than skipping the slot entirely. -->
    <key>StartCalendarInterval</key>
    <array>
        <dict>
            <key>Hour</key><integer>9</integer>
            <key>Minute</key><integer>0</integer>
        </dict>
        <dict>
            <key>Hour</key><integer>14</integer>
            <key>Minute</key><integer>0</integer>
        </dict>
        <dict>
            <key>Hour</key><integer>19</integer>
            <key>Minute</key><integer>0</integer>
        </dict>
    </array>

    <!-- Do NOT set RunAtLoad: that would fire a real upload every time you
         log in, which is not what a three-times-a-day schedule means. -->
    <key>RunAtLoad</key>
    <false/>

    <!-- This is a scheduled job, not a daemon. KeepAlive would restart it
         in a loop forever. -->
    <key>KeepAlive</key>
    <false/>

    <key>ProcessType</key>
    <string>Background</string>

    <!-- run.sh writes its own timestamped log; these catch anything that
         escapes before the wrapper takes over (a bad interpreter path, say). -->
    <key>StandardOutPath</key>
    <string>__PROJECT_DIR__/logs/launchd.out.log</string>
    <key>StandardErrorPath</key>
    <string>__PROJECT_DIR__/logs/launchd.err.log</string>
</dict>
</plist>
